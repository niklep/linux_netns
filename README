vagrantfile and scripts for explaining linux netns

1. clone repo
2. vagrant up
3. run ./linux_netns
4. list netns:
ip netns

5. run commands in netns:
ip netns exec <netns_name> <command_like_ping_or_ip>
